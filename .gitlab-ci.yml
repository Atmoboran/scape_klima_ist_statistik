pages:
  stage: deploy
  script:
    - echo "Publishing static site from public/ (no build step — data is pre-generated and committed)."
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
